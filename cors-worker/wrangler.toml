name = "mianscan-proxy"
main = "worker.js"
compatibility_date = "2024-01-01"

# ES Modules format (export default syntax) — Cloudflare Workers default
# No KV, Durable Objects, or R2 needed — pure fetch proxy
# Free tier: 100,000 requests/day
